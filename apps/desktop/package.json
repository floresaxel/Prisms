{
  "name": "@prisms/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tauri v2 shell wrapping the web build; native SQLite via Tauri SQL plugin (ARCHITECTURE.md §12.3).",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "eslint": "^10.4.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
