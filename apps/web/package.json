{
  "name": "@prisms/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vite + React SPA, PowerSync web SDK (wa-sqlite/OPFS), full feature surface (ARCHITECTURE.md §12).",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@prisms/core": "workspace:*",
    "@prisms/ui": "workspace:*"
  },
  "devDependencies": {
    "eslint": "^10.4.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
