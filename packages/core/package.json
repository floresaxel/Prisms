{
  "name": "@prisms/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure domain logic: status, aggregates, scheduler, rules, graph, commands, time. No IO, no platform APIs (ARCHITECTURE.md §2, §16).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "eslint": "^10.4.1",
    "fast-check": "^4.8.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "fractional-indexing": "^3.2.0",
    "uuid": "^14.0.0",
    "zod": "^4.4.3"
  }
}
